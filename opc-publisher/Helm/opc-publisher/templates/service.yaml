apiVersion: v1
kind: Service
metadata:
  name: {{ include "opc-publisher.fullname" . }}
  labels:
    {{- include "opc-publisher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opc-publisher.selectorLabels" . | nindent 4 }}
