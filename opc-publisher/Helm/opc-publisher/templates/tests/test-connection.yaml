apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opc-publisher.fullname" . }}-test-connection"
  labels:
    {{- include "opc-publisher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opc-publisher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
